import React from "react";
import { Typography } from "@mui/material";

const Login = () => {
  return (
    <Typography
      variant="h5"
      component="h2"
      style={{
        color: "white",
        fontSize: "24px",
        fontStyle: "normal",
        fontWeight: 600,
        lineHeight: "116.7%",
        letterSpacing: "-1.5px",
      }}
    >
      Log in
    </Typography>
  );
};

export default Login;
